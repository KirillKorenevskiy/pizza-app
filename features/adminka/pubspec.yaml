name: adminka
publish_to: 'none'

environment:
  sdk: ^3.6.0
  flutter: ">=3.0.1"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  core_ui:
    path: ../../core_ui
  domain:
    path: ../../domain
  navigation:
    path: ../../navigation
  flutter_svg: ^2.0.9
  image_picker: ^1.0.2
  firebase_core: ^3.10.0
  cloud_firestore: ^5.6.1
  supabase_flutter: ^2.9.0

dev_dependencies:
  build_runner: ^2.4.13
  auto_route_generator: ^7.3.2